# testProject_pino

Консольное Python-приложение для получения прогноза погоды.

Основной проект находится в папке `weather_app`.

```bash
cd weather_app
python -m venv .venv
pip install -r requirements.txt
python app.py
```
